Samira Mantri
Lab 4, Paging
4/17/18

To run my program, simply type "java lab4" followed 
by the 6 command line arguments into the terminal.
Also ensure that the random file is named "random-numbers.txt"
so it can be properly read in. 